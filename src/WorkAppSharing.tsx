import * as React from "react";
import { observer } from "mobx-react";

const WorkAppSharing = (props: {}) => {
  return (
    <div>
      <h1>Work App Sharing</h1>
    </div>
  );
};

export default observer(WorkAppSharing);
